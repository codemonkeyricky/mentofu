// This file provides device definitions for Playwright tests
import { devices } from '@playwright/test';

export const customDevices = {
  // Add custom device definitions here if needed
  // For now, we're using the standard devices from Playwright
};

export { devices };