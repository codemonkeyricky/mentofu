Create a REST API endpoint using Typescript that supports: 
- GET session - gets a session Id and 10 single digit multiplication questions on GET. eg. 2*8, 3*6, ... etc
- POST session - returns answers for the same session. The POST response returns how many questions were correct.

