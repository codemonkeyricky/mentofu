# JWT Secret for authentication (use a strong secret in production)
JWT_SECRET=your_super_secret_jwt_key_here

# Database configuration - using in-memory for development
# For production, set POSTGRES_URL to connect to Vercel Postgres
POSTGRES_URL=